import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
var _excluded = ["children"];
import React from 'react';
import PT from 'prop-types';
import { intrinsicComponent, objectValues } from '../../utils/functions';
import { Size } from './types';
import Styled from './icon-button-table.styles';
var getIconSize = function getIconSize(sizeName) {
  switch (sizeName) {
    case Size.Sm:
      return 12;
    case Size.Md:
    default:
      return 14;
  }
};
var IconButtonTable = intrinsicComponent(function (_ref, ref) {
  var children = _ref.children,
    rest = _objectWithoutProperties(_ref, _excluded);
  return /*#__PURE__*/React.createElement(Styled.IconButtonTable, _extends({}, rest, {
    ref: ref
  }), children && (typeof children === 'function' ? children({
    size: getIconSize(rest.size)
  }) : children));
});
IconButtonTable.defaultProps = {
  size: Size.Md
};
IconButtonTable.propTypes = {
  size: PT.oneOf(objectValues(Size))
};
export default IconButtonTable;